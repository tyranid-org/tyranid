import * as React from 'react';
import { Route } from 'react-router-dom';

//export type TyrAdminProps = Readonly<{}>;

// <TyrKanban data={kanbanData}/>
//export const TyrAdmin = (props: TyrAdminProps) => {
/*

     What does this look like?

        ?. does it have its own menuing system?

        ?. does it just add a bunch of routes and let the client deal with menuing?


   */
//};

//export const adminRoutes = () => {};
